the bare min app that can be deployed with harness that demonstrates the way harness can deploy a multi-layer application.

Frontend
Backend
Database

layer cake - mern
layer cake - ruby

We need an application that has a frontend that interacts with the backend to persist and retrieve data from the database.
